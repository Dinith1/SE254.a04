<%@ jet package="se254.a4.q2" class="UnitTestTemplate" imports="java.lang.reflect.*" %> 
import static org.junit.Assert.*;
import org.junit.Test;

<% Class cl = (Class) argument; %>
public class <%=cl.getSimpleName()%>Test {
	<% for (Method m : cl.getDeclaredMethods()) { %>
		@Test
		public void test<%=m.getName().substring(0,1).toUpperCase() + m.getName().substring(1)%>() {
			fail("Not yet implemented");
		}
	<%}%>
}
